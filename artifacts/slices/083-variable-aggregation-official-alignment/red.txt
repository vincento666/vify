RED gate: variable aggregation official alignment.

Reference basis:
- AgentArts official variable aggregation node: used to aggregate outputs from
  multiple branches, using the aggregation strategy "返回每个分组中第一个非空的值".
- Local Coze Studio source: `VariableMergeForm` renders `MergeStrategyField`,
  `MergeGroupsField`, and read-only outputs; `MergeGroup` data is
  `{ name, variables }`.

Commands and expected failures before fix:

1. Frontend schema:
rtk npm --prefix frontend run test:unit -- src/views/workflow/nodeConfig.test.ts

Failure:
expected [ '输入', '聚合规则', '输出', '高级/兼容配置' ] to deeply equal
[ '聚合策略', '变量分组', '输出', '高级/兼容配置' ]

2. Backend grouped runtime:
rtk .venv/bin/python -m pytest tests/integration/workflow/test_variable_aggregation_assignment.py -q

Failure:
expected {"final": "Group1=vip refund;Group2=CN"} but got
{"final": "Group1=;Group2="}

3. E2E panel:
rtk node frontend/e2e/workflow-variable-aggregation-official.mjs

Failure:
Expected only first non-empty group strategy copy

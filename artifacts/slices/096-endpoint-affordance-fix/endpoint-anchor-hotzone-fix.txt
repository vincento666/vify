Slice: endpoint anchor hotzone fix

Problem:
- Enlarging the whole VueFlow handle made edge anchors attach to the invisible hitbox edge, so connection lines did not visually touch the endpoint dot.
- Hover size should stay restored; only the hit target should be larger.

Fix:
- Keep `.node-port` handle box equal to the visible dot size (`1rem`).
- Move the `3rem` hover/connection hit target into `::before`.
- Keep source/target offsets based on `--node-port-dot-size` so VueFlow edge anchors line up with the visible endpoint dot.

Gates:
- `rtk npm --prefix frontend run test:unit -- src/views/workflow/canvasControls.test.ts src/views/workflow/workflowCanvasRemGovernance.test.ts` PASS
- `rtk npm --prefix frontend run test:unit -- src/remScaleClosure.test.ts` PASS
- `rtk node frontend/e2e/chatflow-endpoint-affordances.mjs` PASS, chatflow=8174
- `rtk node frontend/e2e/chatflow-endpoint-connection-radius.mjs` PASS, chatflow=8175

Browser UAT:
- Current in-app page: `http://127.0.0.1:5173/workflows/6217/canvas`
- Measured three rendered edges after reload.
- Source/target horizontal gaps between edge path rect and matching endpoint dot outer edge were 0 to 0.000061px.
- Vertical gaps to endpoint centers were approximately 0.000015px.
- Result: connection lines visually touch the endpoint dots; hover sizes are not enlarged beyond the existing 2x/3x rules.

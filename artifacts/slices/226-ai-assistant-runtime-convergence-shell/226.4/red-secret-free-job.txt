Slice: 226.4 secret-free runtime job payload
Date: 2026-07-18

Command:
rtk uv run pytest tests/contract/runtime_jobs/test_secret_free_payload.py -q --tb=short

Observed RED:
RuntimeJobRepository persisted a nested raw credential-like field and created a
runtime_jobs row.

Meaning:
The durable substrate had no last-line guard against storing raw secrets.
Explicit credential references and raw credential values were indistinguishable.

Command: rtk node frontend/e2e/workflow-endpoint-magnetic-preview.mjs
Date: 2026-06-09
Result: PASS

Coverage:
- Opens workflow canvas with START and END nodes.
- Drags from the START source endpoint toward the END target endpoint.
- Moves the pointer to 16px from the target endpoint center, inside the 18px magnetic connection radius.
- Asserts the END target endpoint enters `connection-preview`.
- Parses the endpoint dot pseudo-element transform matrix and asserts scale > 1.05, proving it visually scales during magnetic preview.
- Releases the pointer and verifies the edge is created.

Screenshot command:
HIFY_E2E_SCREENSHOT=artifacts/slices/120-endpoint-magnetic-preview/uat-endpoint-magnetic-preview.png rtk node frontend/e2e/workflow-endpoint-magnetic-preview.mjs

Screenshot result: PASS
Note: PNG screenshots are ignored by repository policy (`artifacts/**/*.png`) but remain available locally for UAT.

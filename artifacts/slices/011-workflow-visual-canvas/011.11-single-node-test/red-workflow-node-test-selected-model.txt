Command:
rtk node frontend/e2e/workflow-node-test.mjs

Expected RED:
Selected LLM node test should run with the node's configured `modelConfigId` and finish successfully.

Failure:
locator.waitFor: Timeout 30000ms exceeded.
Call log:
  - waiting for locator('[data-testid="node-test-drawer"]').getByText('SUCCEEDED') to be visible

Diagnosis:
The workflow node config includes `modelConfigId`, but the backend LLM completer currently builds requests from the default live Agent model config only.

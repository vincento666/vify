Command:
rtk .venv/bin/python -m pytest tests/unit/workflow/test_service_provider_llm.py -q

Expected RED:
`test_run_node_prefers_llm_node_model_config_over_default_agent` must fail before implementation because the selected node's `modelConfigId` is ignored.

Failure:
AssertionError: Lists differ: [601] != [601, 777]

Meaning:
The backend fetched only the default Agent model config and never fetched the LLM node's configured model config.

Slice: selected-node LLM model config

Problem:
- LLM node config saved `modelConfigId`, but node-level run still used the default live Agent model config.
- UI selected-node test could fail with a real provider DNS/network error even when the node selected an enabled `mock://success` model.

Fix:
- `_llm_options` now carries `modelConfigId` / `model_config_id`.
- Workflow LLM completer resolves the active model config from node options before building payload/client/fallback params.
- Default Agent prompt/temperature/max_tokens remain the fallback execution profile.

Gates:
- `rtk .venv/bin/python -m pytest tests/unit/workflow/test_service_provider_llm.py -q` PASS, 9 passed
- `rtk .venv/bin/python -m pytest tests/integration/workflow/test_workflow_node_run.py tests/integration/workflow/test_workflow_llm_resources.py -q` PASS, 5 passed
- Direct node run API after backend restart returned HTTP 200 / `SUCCEEDED` with selected mock model.
- `rtk node frontend/e2e/workflow-node-test.mjs` PASS

Runtime note:
- Restarted local uvicorn on `127.0.0.1:8000` so e2e used current backend code.

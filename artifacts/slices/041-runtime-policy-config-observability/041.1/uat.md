# UAT

- Spec: 041-runtime-policy-config-observability
- Slice: 041.1 Runtime policy profile model and API
- URL: http://127.0.0.1:8041/docs
- Browser: Codex in-app browser
- Steps:
  - Started FastAPI with isolated SQLite DB.
  - Created RuntimePolicyProfile through live HTTP API.
  - Listed profiles through live HTTP API and confirmed persisted profile.
  - Opened Swagger UI in real browser and confirmed `runtime-policy` endpoints are present.
- Expected:
  - Profile create/list returns `{code,message,data}` envelope.
  - Swagger exposes backend-only RuntimePolicyProfile endpoints.
- Actual:
  - `createdCode=200`, `createdVersion=1`, `total=1`.
  - Browser page title: `Hify - Swagger UI`.
  - Browser snapshot contained `runtime-policy`.
- Screenshots:
  - `screenshots/browser-swagger-runtime-policy.png`
- Verdict: PASS

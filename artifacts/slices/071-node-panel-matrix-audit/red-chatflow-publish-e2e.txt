RED: frontend/e2e/chatflow-publish.mjs

Observed failure before the E2E was aligned with the current Chatflow publish flow:

Error: Expected publish panel to show successful run status

Root causes:

- The old script still treated publish as a mixed operations side panel.
- It expected the trial-run form to use `输入用户消息` and `运行`, but the product now uses the chat-style input `输入消息` and `发送消息`.
- The first publish gate modal was closed with `Escape`, which could leave a stale publish dialog attached; the test then read the stale pre-run gate instead of the post-run publish dialog.

Expected behavior:

- Publish opens `workflow-publish-dialog`.
- Open API lives under the `开放` lifecycle tab.
- Chatflow trial run uses chat input and send button.
- The first publish dialog is closed via `取消` and waited hidden before continuing.

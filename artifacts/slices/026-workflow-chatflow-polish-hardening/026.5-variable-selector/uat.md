# 026.5 Browser UAT

- Canvas: `http://127.0.0.1:5173/chatflows/create`
- Verified: LLM input rows expose direct literal input, no visible reference/literal mode selector, variable picker stays inside the config panel, and selecting `{{sys.query}}` renders a compact chip.
- Screenshot: `uat-variable-selector.png`

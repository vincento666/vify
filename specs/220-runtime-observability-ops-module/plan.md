# Plan — Spec 220

## 实施策略

按 RED → GREEN → REFACTOR 串行推进 slice 列表。每个 slice 必须先写红测证据再写最小实现。

- 220.1 先把入口做出来，让后续 slice 的页面挂得上。
- 220.2 → 220.5 由"概览 → 详情 → 节点 → 任务/Worker"递进。
- 220.6 / 220.7 做 DLQ 与安全运维动作。
- 220.8 / 220.9 做统计与实时更新。
- 220.10 出口回归。

## 关键风险

- 风险 1：观测面板字段如果新增，会让 spec 213-219 暴露的接口被回改。
  - 缓解：220 阶段禁止新增前序未暴露的字段；本 spec 通过聚合 + 渲染解决。
- 风险 2：实时更新对 SSE 通道压力大。
  - 缓解：220.9 复用 spec 219 的 SSE / 压缩 / 断线恢复；面板默认仅订阅可视范围内的 run。
- 风险 3：安全运维动作误操作风险。
  - 缓解：220.7 强制二次确认 + 操作日志写 audit 表；高风险动作 e2e + UAT 双覆盖。
- 风险 4：rem 与新页面布局冲突。
  - 缓解：所有视觉尺寸用 rem；新增页面必须跑 remScaleClosure。

## 与其他 spec 的相互影响

- 与 spec 213-219：本 spec 不得新增字段；仅复用、聚合、展示。
- 与 spec 221：本 spec 完成后 221 才能用面板做容量演练观察。

## 工程任务序列

1. 220.1 → 220.2 → 220.3 → 220.4 → 220.5 → 220.6 → 220.7 → 220.8 → 220.9 → 220.10。

## 出口条件（Definition of Done for the whole spec）

- 主菜单存在独立"运行观测"模块入口。
- Run 列表 / 详情 / DAG 图 / Node 详情 / Job 队列 / Worker 心跳 / DLQ / 统计 / 失败重试 全部有前端单测 + e2e + UAT 截图。
- 4 类安全运维动作有二次确认 + e2e + UAT。
- 实时更新走 spec 219 SSE，断线可恢复。
- spec 212-219 入口门禁回归全绿。

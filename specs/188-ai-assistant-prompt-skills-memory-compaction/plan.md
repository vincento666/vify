# Plan

1. Add RED unit coverage for optional Phase 5 prompt layers.
2. Add a small read-only skill registry domain model.
3. Extend `PromptAssembler` optional inputs without changing existing default
   layer order.
4. Run focused unit, lint, type, AI Assistant kernel regression, and boundary
   scans.
5. Save evidence under
   `artifacts/slices/188-ai-assistant-prompt-skills-memory-compaction/188.1/`.


# Plan

1. Add RED unit and contract tests for missing
   `customer_assistant_subagent_bridge`.
2. Implement a read-only tool manifest and deterministic handler in the AI
   Assistant tool registry.
3. Reuse existing customer-assistant harness adapter helpers for public refs.
4. Run unit, MySQL8 contract, customer-assistant harness compatibility, ruff,
   mypy, regression, and boundary-scan gates.
5. Save evidence under
   `artifacts/slices/189-ai-assistant-customer-assistant-subagent-bridge/`.


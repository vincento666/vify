# Plan

1. Add RED unit and contract tests for missing observability snapshot.
2. Implement backend `build_observability_snapshot`.
3. Attach snapshot to run inspector while preserving existing `usage`.
4. Run focused unit, MySQL8 contract, inspector/e2e regressions, ruff, mypy,
   and boundary scan.
5. Save evidence under
   `artifacts/slices/190-ai-assistant-observability-benchmark/`.


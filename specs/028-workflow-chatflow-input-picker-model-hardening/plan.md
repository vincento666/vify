# 028 Plan

1. 028.1 Inline variable trigger: `{ -> {{}}`、光标居中、Esc 关闭、删除可编辑。
2. 028.2 Variable picker shell: 重构为左侧展开的 Coze-like 分组选择器。
3. 028.3 LLM model search: 模型弹层替换 mock 搜索文案为真实搜索输入和按钮。
4. 028.4 Model parameter sliders: 数值参数使用长条滑轨 + 圆点滑块 + 可编辑/可微调数值输入框。
5. Final gates: unit、build、focused e2e、rem、browser UAT。
